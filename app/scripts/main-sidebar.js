'use strict';

require(['cards-controller',
         'youtube-channel-subscribe-directive',
         'description-filter',
         'numsmall-filter',
         'trust-resource-url-filter'], function() {
    angular.bootstrap(document, ['app']);
});
